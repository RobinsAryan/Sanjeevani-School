<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Result | <%=data.result.userId.username%>
    </title>
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="/css/student/attandance.css">
    <link rel="stylesheet" href="/css/result.css">
    <%if(isPrinciple){%>
        <link rel="stylesheet" href="/css/principle/editMark.css"> 
        <%}%>
    <link rel="stylesheet" href="/css/popup.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://kit.fontawesome.com/c538c9717e.js" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
</head>

<body>
    <input type="text" id="userId" hidden value="<%=data.result.userId._id%>">
    <input type="text" id="resultId" hidden value="<%=data._id%>">
    <input type="number" id="MM" hidden value="<%=data.MM%>">
    <div class="main">
        <div class="view">
            <div class="header">
                <span class="pageTitle">Result</span>
                <div class="rightHeader">
                    <span>
                        <i onclick="history.back()" class="fas fa-arrow-left"></i>
                    </span>
                </div>
                <div class="leftHeader">
                    <div onclick="openUserProfile()">
                        <%if(data.profile){%>
                            <img src="<%=data.profile%>" alt="">
                            <%}else{%>
                                <img src="/img/nouser.png" alt="">
                                <%}%>
                    </div>
                    </p>
                </div>
            </div>
            <div class="resultTable">
                <div class="resultInfo">
                    <p>Exam: <%=data.title%>
                    </p>
                    <span>Uploaded On: <%=data.createdAt%></span>
                </div>
                <%if(isPrinciple){%>
                    <button class="normalButton" style="margin-top: 15px;" onclick="openEditMarks()">Edit Result</button>
                    <%}%>
                        <div id="mainResult">

                        </div>

            </div>
            <div class="chart1">

            </div>
        </div>
    </div>
    <div id="popup" class="popup">

    </div>
    <script src="/js/common.js"></script>
    <script src="/js/result.js"></script>
    <%if(isPrinciple){%>
        <script src="/js/principle/editMarks.js"></script>
        <%}%>
</body>

</html>